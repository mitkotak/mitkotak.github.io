function w(e,t=!1){switch(e){case"bool":return t?"i32":"bool";case"int32":return"i32";case"uint32":return"u32";case"float32":return"f32";case"float16":return"f16";default:throw new Error(`Unsupported dtype for WebGPU: ${e}`)}}function K(e){switch(e){case"bool":return"1";case"int32":return"2147483647";case"uint32":return"4294967295u";case"float32":return"inf()";case"float16":return"f16(inf())";default:throw new Error(`Unsupported dtype for WebGPU: ${e}`)}}function Q(e,t){if("bool"===e)return t?"true":"false";if("int32"===e)return t.toString();if("uint32"===e)return t.toString()+"u";if("float32"===e)return Number.isNaN(t)?"nan()":Number.isFinite(t)?"f32("+t.toString()+")":t>0?"inf()":"-inf()";if("float16"===e)return Number.isNaN(t)?"f16(nan())":Number.isFinite(t)?"f16("+t.toString()+")":t>0?"f16(inf())":"f16(-inf())";throw new Error(`Unsupported const dtype: ${e}`)}function le(e,t,n,r){if("Add"===e)return`(${n} + ${r})`;if("Mul"===e)return`(${n} * ${r})`;if("Min"===e)return"bool"===t?`(${n} && ${r})`:`min(${n}, ${r})`;if("Max"===e)return"bool"===t?`(${n} || ${r})`:`max(${n}, ${r})`;throw new Error(`Unsupported reduction op: ${e}`)}function E(e){let t=e,n=1;return e>65535&&(t=M,n=Math.ceil(e/M)),[t,n]}function ce(e){return"float16"===e?"float32":"bool"===e?"int32":e}function fe({name:e,dtype:t,uniformDtype:n}){const r=U.variable(n,`uniforms.${e}`);return"float16"===t?U.cast("float16",r):"bool"===t?U.cmpne(r,U.i32(0)):r}function pe(e,t,n,r){switch(n){case"float32":e.setFloat32(t,r,!0);break;case"int32":e.setInt32(t,r,!0);break;case"uint32":e.setUint32(t,r,!0);break;default:throw new Error(`Unsupported dtype for constant uniform: ${n}`)}}function de(e){const t=[];return[e.rewrite((e=>{if("Const"!==e.op||0===e.arg)return;const n={name:`c${t.length}`,dtype:e.dtype,uniformDtype:ce(e.dtype),value:e.arg};return t.push(n),fe(n)})),t]}function ge(e){const t=new Uint8Array(4*e.length),n=new DataView(t.buffer);return e.forEach(((e,t)=>pe(n,4*t,e.uniformDtype,e.value))),t}function me(e,t){if(0!==t.nargs||t.reduction)return null;let n=t.exp.substitute({gidx:U.special("int32","gidx",t.size)}).simplify(),r=[];[n,r]=de(n);const i=new V;i.emitPreamble(e,[n]),r.length>0&&i.emit("struct Uniforms {",i.pushIndent,...r.map((e=>`${e.name}: ${w(e.uniformDtype)},`)),i.popIndent,"}\n");const a=w(t.dtype,!0);i.emit(`@group(0) @binding(0) var<storage, read_write> result : array<${a}>;`),r.length>0&&i.emit("@group(1) @binding(0) var<uniform> uniforms: Uniforms;");const s=C(t.size,256),[o,u]=E(Math.ceil(t.size/s));if(i.emit("",`@compute @workgroup_size(${s})`,"fn main(@builtin(global_invocation_id) id : vec3<u32>) {",i.pushIndent),1===u)i.emit(`if (id.x >= ${t.size}) { return; }`,"let gidx: i32 = i32(id.x);");else{const e=o*s;i.emit(`if (${e} * id.y + id.x >= ${t.size}) { return; }`,`let gidx: i32 = i32(${e} * id.y + id.x);`)}const l=new Z(i,[]);l.countReferences(n);let d=h(l.run(n));return a!==w(n.dtype)&&(d=`${a}(${d})`),i.emit(`result[gidx] = ${d};`,i.popIndent,"}"),{code:i.toString(),numInputs:0,numOutputs:1,hasUniform:r.length>0,passes:[{grid:[o,u],uniform:r.length>0?ge(r):void 0}]}}function he(e){const t=new Uint32Array(3);return t[0]="sort"===e.kind?0:1,t[1]=e.mergeStep??0,t[2]=e.mergeStage??0,new Uint8Array(t.buffer)}function J(e,t,n,r,i){const a=w(t,!0),s=1<<Math.ceil(Math.log2(n||1)),o=Math.ceil(s/2),u=C(o,e.limits.maxComputeWorkgroupSizeX),l=o/u,d=Math.log2(s),p=Math.min(d,Math.log2(2*u)),c="float16"===t,f=R(t)?`${a}(nan())`:K(t),h=`\n${c?"enable f16;":""}\n${z}\n\nstruct Uniforms {\n  kind: u32, // 0 = sort, 1 = merge\n  merge_step: u32, // half_block = 2^step\n  merge_stage: u32, // only used for merge\n}\n\n@group(0) @binding(0) var<storage, read> input: array<${a}>;\n@group(0) @binding(1) var<storage, read_write> output: array<${a}>;\n${i?"@group(0) @binding(2) var<storage, read_write> output_idx: array<i32>;":""}\n\n@group(1) @binding(0) var<uniform> uniforms: Uniforms;\n\nvar<workgroup> shared_vals: array<${a}, ${2*u}>;\n${i?`var<workgroup> shared_idx: array<i32, ${2*u}>;`:""}\n\nfn compare(a: ${a}, b: ${a}) -> bool {\n${R(t)?"\n  let min_value = min(a, b);\n  return a == min_value && b != min_value;":"  return a < b;"}\n}\n\nfn compare_and_swap(i: u32, j: u32) {\n  let val_i = shared_vals[i];\n  let val_j = shared_vals[j];\n${i?"\n  if (\n    compare(val_j, val_i) ||\n    (!compare(val_i, val_j) && shared_idx[j] < shared_idx[i])\n  ) {\n    shared_vals[i] = val_j;\n    shared_vals[j] = val_i;\n    let tmp_idx = shared_idx[i];\n    shared_idx[i] = shared_idx[j];\n    shared_idx[j] = tmp_idx;\n  }":"\n  if (compare(val_j, val_i)) {\n    shared_vals[i] = val_j;\n    shared_vals[j] = val_i;\n  }"}\n}\n\n@compute @workgroup_size(${u})\nfn main(\n  @builtin(workgroup_id) wg_id: vec3<u32>,\n  @builtin(local_invocation_id) local_id: vec3<u32>,\n) {\n  let blockid = wg_id.x + wg_id.y * ${M}u;\n  let batch = blockid / ${l}u;\n  let wg_in_batch = blockid % ${l}u;\n\n  let tid = local_id.x;\n  let base = batch * ${n}u;\n\n  if (uniforms.kind == 0u || (uniforms.kind == 1u && uniforms.merge_step == ${p-1}u)) {\n    let wg_base = wg_in_batch * ${2*u}u;\n\n    // Load data into shared memory (2 elements per thread)\n    let idx0 = tid * 2u;\n    let idx1 = tid * 2u + 1u;\n    // Load from input for initial 'sort' pass, then from output (read-write) for 'merge' passes.\n    if (uniforms.kind == 0u) {\n      shared_vals[idx0] = select(${f}, input[base + wg_base + idx0], wg_base + idx0 < ${n}u);\n      shared_vals[idx1] = select(${f}, input[base + wg_base + idx1], wg_base + idx1 < ${n}u);\n${i?"\n      shared_idx[idx0] = i32(wg_base + idx0);\n      shared_idx[idx1] = i32(wg_base + idx1);":""}\n    } else {\n      shared_vals[idx0] = select(${f}, output[base + wg_base + idx0], wg_base + idx0 < ${n}u);\n      shared_vals[idx1] = select(${f}, output[base + wg_base + idx1], wg_base + idx1 < ${n}u);\n${i?`\n      shared_idx[idx0] = select(${n}, output_idx[base + wg_base + idx0], wg_base + idx0 < ${n}u);\n      shared_idx[idx1] = select(${n}, output_idx[base + wg_base + idx1], wg_base + idx1 < ${n}u);`:""}\n    }\n    workgroupBarrier();\n\n    let initial_stage = select(0u, ${p-1}u, uniforms.kind != 0u);\n    for (var stage = initial_stage; stage < ${p}u; stage++) {\n      for (var step1 = stage + 1u; step1 > 0u; step1--) {\n        let step = step1 - 1u;\n        let half_block = 1u << step;\n        let is_first_step = uniforms.kind == 0u && step == stage;\n\n        let block_offset = (tid / half_block) * half_block;\n        let local_offset = tid % half_block;\n        let i = block_offset * 2u + local_offset;\n        let j = select(i + half_block, i ^ (half_block * 2u - 1u), is_first_step);\n        compare_and_swap(i, j);\n\n        workgroupBarrier();\n      }\n    }\n\n    if (wg_base + idx0 < ${n}u) {\n      output[base + wg_base + idx0] = shared_vals[idx0];\n      ${i?"output_idx[base + wg_base + idx0] = shared_idx[idx0];":""}\n    }\n    if (wg_base + idx1 < ${n}u) {\n      output[base + wg_base + idx1] = shared_vals[idx1];\n      ${i?"output_idx[base + wg_base + idx1] = shared_idx[idx1];":""}\n    }\n  } else {\n    // Execute single merge pass for a step >= numLocalStages.\n    let half_block = 1u << uniforms.merge_step;  // half_block >= workgroupSize * 2\n    let thread_in_batch = wg_in_batch * ${u} + tid;\n    let is_first_step = uniforms.merge_step == uniforms.merge_stage;\n\n    let block_offset = (thread_in_batch / half_block) * half_block;\n    let local_offset = thread_in_batch % half_block;\n    let i = block_offset * 2u + local_offset;\n    let j = select(i + half_block, i ^ (half_block * 2u - 1u), is_first_step);\n\n    // Global version of compare_and_swap()\n    if (j < ${n}u) {\n      let val_i = output[base + i];\n      let val_j = output[base + j];\n${i?"\n      let idx_i = output_idx[base + i];\n      let idx_j = output_idx[base + j];\n      if (compare(val_j, val_i) || (!compare(val_i, val_j) && idx_j < idx_i)) {\n        output[base + i] = val_j;\n        output[base + j] = val_i;\n        output_idx[base + i] = idx_j;\n        output_idx[base + j] = idx_i;":"\n      if (compare(val_j, val_i)) {\n        output[base + i] = val_j;\n        output[base + j] = val_i;"}\n      }\n    }\n  }\n}\n`.trim(),x=E(r*l),m=[{kind:"sort"}];for(let e=p;e<d;e++)for(let t=e;t>=p-1;t--)m.push({kind:"merge",mergeStep:t,mergeStage:e});return[{code:h,numInputs:1,numOutputs:i?2:1,hasUniform:!0,passes:m.map((e=>({grid:x,uniform:he(e)})))}]}function _e(e,t){const n=t.inputDtypes[0],r=t.inputShapes[0];return J(e,n,r[r.length-1],q(r.slice(0,-1)),!1)}function $e(e,t){const n=t.inputDtypes[0],r=t.inputShapes[0];return J(e,n,r[r.length-1],q(r.slice(0,-1)),!0)}function be(e,t,n){const r=t.inputDtypes[0],i=t.inputShapes[0],a=t.inputShapes[1],s=i[i.length-1],o=a[a.length-2],u=q(i.slice(0,-2)),l="float16"===r,d=w(r,!0),p=C(s,e.limits.maxComputeWorkgroupSizeX);return[{code:`\n${l?"enable f16;":""}\n${z}\n\n@group(0) @binding(0) var<storage, read> a: array<${d}>;\n@group(0) @binding(1) var<storage, read> b: array<${d}>;\n@group(0) @binding(2) var<storage, read_write> x: array<${d}>;\n\n// Shared memory for the current pivot value x[j]\nvar<workgroup> x_j: ${d};\n\n@compute @workgroup_size(${p})\nfn main(\n  @builtin(workgroup_id) wg_id: vec3<u32>,\n  @builtin(local_invocation_id) local_id: vec3<u32>,\n) {\n  let wg_idx = wg_id.x + wg_id.y * ${M}u;\n  let mat_idx = wg_idx / ${o}u;\n  let rhs_idx = wg_idx % ${o}u;\n\n  if (mat_idx >= ${u}u) {\n    return;\n  }\n\n  let a_base = mat_idx * ${s*s}u;\n  let bx_base = (mat_idx * ${o}u + rhs_idx) * ${s}u;\n  let tid = local_id.x;\n\n  // Step 1: Copy b to x (threads collaborate)\n  for (var idx = tid; idx < ${s}u; idx += ${p}u) {\n    x[bx_base + idx] = b[bx_base + idx];\n  }\n  storageBarrier();\n\n  // Step 2: Back-substitution from j = n-1 down to 0\n  for (var jj = 0u; jj < ${s}u; jj++) {\n    let j = ${s-1}u - jj;\n\n    // Thread 0 computes x[j] = x[j] / a[j,j]\n    if (tid == 0u) {\n      ${n.unitDiagonal?"x_j = x[bx_base + j];":`x_j = x[bx_base + j] / a[a_base + j * ${s}u + j];`}\n      x[bx_base + j] = x_j;\n    }\n    workgroupBarrier();  // Sync shared memory x_j\n\n    // All threads subtract x[j] * a[i,j] from x[i] for i < j\n    for (var i = tid; i < j; i += ${p}u) {\n      x[bx_base + i] -= x_j * a[a_base + i * ${s}u + j];\n    }\n    workgroupBarrier();\n    storageBarrier();\n  }\n}\n`.trim(),numInputs:2,numOutputs:1,hasUniform:!1,passes:[{grid:E(u*o)}]}]}function we(e,t){const n=t.inputDtypes[0],r=t.inputShapes[0],i=r[r.length-1],a=q(r.slice(0,-2)),s="float16"===n,o=w(n,!0),u=C(i,e.limits.maxComputeWorkgroupSizeX);return[{code:`\n${s?"enable f16;":""}\n${z}\n\n@group(0) @binding(0) var<storage, read> input: array<${o}>;\n@group(0) @binding(1) var<storage, read_write> output: array<${o}>;\n\n// Shared memory for the diagonal element\nvar<workgroup> L_jj: ${o};\n\n@compute @workgroup_size(${u})\nfn main(\n  @builtin(workgroup_id) wg_id: vec3<u32>,\n  @builtin(local_invocation_id) local_id: vec3<u32>,\n) {\n  let batch = wg_id.x + wg_id.y * ${M}u;\n  if (batch >= ${a}u) {\n    return;\n  }\n\n  let base = batch * ${i*i}u;\n  let tid = local_id.x;\n\n  // Zero out output and copy lower triangle from input (threads collaborate)\n  for (var idx = tid; idx < ${i*i}u; idx += ${u}u) {\n    let row = idx / ${i}u;\n    let col = idx % ${i}u;\n    output[base + idx] = select(0, input[base + idx], col <= row);\n  }\n  storageBarrier();\n\n  // Cholesky-Crout algorithm: process column by column\n  for (var j = 0u; j < ${i}u; j++) {\n    // Step 1: All threads compute sum for their rows i >= j in parallel\n    // sum = A[i][j] - sum(L[i][k] * L[j][k] for k < j)\n    for (var i = j + tid; i < ${i}u; i += ${u}u) {\n      var sum = output[base + i * ${i}u + j];\n      for (var k = 0u; k < j; k++) {\n        sum -= output[base + i * ${i}u + k] * output[base + j * ${i}u + k];\n      }\n      output[base + i * ${i}u + j] = sum;\n    }\n    storageBarrier();\n\n    // Step 2: Thread 0 computes L[j][j] = sqrt(output[j][j])\n    if (tid == 0u) {\n      L_jj = sqrt(output[base + j * ${i}u + j]);\n      output[base + j * ${i}u + j] = L_jj;\n    }\n    workgroupBarrier();\n\n    // Step 3: All threads divide output[i][j] by L[j][j] for i > j\n    for (var i = j + 1u + tid; i < ${i}u; i += ${u}u) {\n      output[base + i * ${i}u + j] /= L_jj;\n    }\n    storageBarrier();\n  }\n}\n`.trim(),numInputs:1,numOutputs:1,hasUniform:!1,passes:[{grid:E(a)}]}]}function ve(e,t){const n=t.inputDtypes[0],r=t.inputShapes[0],i=r[r.length-2],a=r[r.length-1],s=Math.min(i,a),o=q(r.slice(0,-2)),u="float16"===n,l=w(n,!0),d=C(Math.max(i,a),e.limits.maxComputeWorkgroupSizeX);return[{code:`\n${u?"enable f16;":""}\n${z}\n\n@group(0) @binding(0) var<storage, read> input: array<${l}>;\n@group(0) @binding(1) var<storage, read_write> lu: array<${l}>;\n@group(0) @binding(2) var<storage, read_write> pivots: array<i32>;\n@group(0) @binding(3) var<storage, read_write> perm: array<i32>;\n\nvar<workgroup> pivot_row: u32;\nvar<workgroup> pivot_val: ${l};\n\n@compute @workgroup_size(${d})\nfn main(\n  @builtin(workgroup_id) wg_id: vec3<u32>,\n  @builtin(local_invocation_id) local_id: vec3<u32>,\n) {\n  let batch = wg_id.x + wg_id.y * ${M}u;\n  if (batch >= ${o}u) {\n    return;\n  }\n\n  let lu_base = batch * ${i*a}u;\n  let piv_base = batch * ${s}u;\n  let perm_base = batch * ${i}u;\n  let tid = local_id.x;\n\n  // Copy input to lu\n  for (var idx = tid; idx < ${i*a}u; idx += ${d}u) {\n    lu[lu_base + idx] = input[lu_base + idx];\n  }\n  // Initialize permutation\n  for (var idx = tid; idx < ${i}u; idx += ${d}u) {\n    perm[perm_base + idx] = i32(idx);\n  }\n  storageBarrier();\n\n  // LU decomposition with partial pivoting\n  for (var j = 0u; j < ${s}u; j++) {\n    // Step 1: Thread 0 finds pivot (max abs value in column j, rows >= j)\n    if (tid == 0u) {\n      var max_val = abs(lu[lu_base + j * ${a}u + j]);\n      var max_row = j;\n      for (var i = j + 1u; i < ${i}u; i++) {\n        let val = abs(lu[lu_base + i * ${a}u + j]);\n        if (val > max_val) {\n          max_val = val;\n          max_row = i;\n        }\n      }\n      pivot_row = max_row;\n      pivot_val = lu[lu_base + max_row * ${a}u + j];\n      pivots[piv_base + j] = i32(max_row);\n    }\n    workgroupBarrier();\n\n    // Step 2: Swap rows j and pivot_row (threads collaborate)\n    let pr = pivot_row;\n    if (pr != j) {\n      for (var col = tid; col < ${a}u; col += ${d}u) {\n        let tmp = lu[lu_base + j * ${a}u + col];\n        lu[lu_base + j * ${a}u + col] = lu[lu_base + pr * ${a}u + col];\n        lu[lu_base + pr * ${a}u + col] = tmp;\n      }\n      if (tid == 0u) {\n        let tmp_p = perm[perm_base + j];\n        perm[perm_base + j] = perm[perm_base + pr];\n        perm[perm_base + pr] = tmp_p;\n      }\n    }\n    storageBarrier();\n\n    // Step 3: Compute L[i][j] and update submatrix\n    // Each thread handles one row i > j\n    for (var i = j + 1u + tid; i < ${i}u; i += ${d}u) {\n      let factor = lu[lu_base + i * ${a}u + j] / pivot_val;\n      lu[lu_base + i * ${a}u + j] = factor; // L[i][j]\n      for (var k = j + 1u; k < ${a}u; k++) {\n        lu[lu_base + i * ${a}u + k] -= factor * lu[lu_base + j * ${a}u + k];\n      }\n    }\n    storageBarrier();\n  }\n}\n`.trim(),numInputs:1,numOutputs:3,hasUniform:!1,passes:[{grid:E(o)}]}]}function ye(e,t,n){const r=t.inputDtypes[0],i=t.inputShapes[0],a=i[i.length-1],s=q(i.slice(0,-2)),o="float16"===r,u=w(r,!0),l=`${u}(${n.tolerance})`,d=C(Math.max(a,1),e.limits.maxComputeWorkgroupSizeX);return[{code:`\n${o?"enable f16;":""}\n${z}\n\n@group(0) @binding(0) var<storage, read> input: array<${u}>;\n@group(0) @binding(1) var<storage, read_write> diagonalized: array<${u}>;\n@group(0) @binding(2) var<storage, read_write> vectors: array<${u}>;\n\nvar<workgroup> done: u32;\nvar<workgroup> rot_active: u32;\nvar<workgroup> rot_c: ${u};\nvar<workgroup> rot_s: ${u};\nvar<workgroup> rot_app: ${u};\nvar<workgroup> rot_aqq: ${u};\nvar<workgroup> rot_apq: ${u};\n\nfn mat_idx(base: u32, row: u32, col: u32) -> u32 {\n  return base + row * ${a}u + col;\n}\n\nfn sym_idx(base: u32, row: u32, col: u32) -> u32 {\n  return mat_idx(base, max(row, col), min(row, col));\n}\n\n@compute @workgroup_size(${d})\nfn main(\n  @builtin(workgroup_id) wg_id: vec3<u32>,\n  @builtin(local_invocation_id) local_id: vec3<u32>,\n) {\n  let batch = wg_id.x + wg_id.y * ${M}u;\n  if (batch >= ${s}u) {\n    return;\n  }\n\n  let base = batch * ${a*a}u;\n  let tid = local_id.x;\n\n  for (var idx = tid; idx < ${a*a}u; idx += ${d}u) {\n    let row = idx / ${a}u;\n    let col = idx % ${a}u;\n    diagonalized[base + idx] = select(\n      ${u}(0),\n      input[base + idx],\n      row >= col,\n    );\n    vectors[base + idx] = select(${u}(0), ${u}(1), row == col);\n  }\n  storageBarrier();\n\n  for (var sweep = 0u; sweep < ${n.maxSweeps}u; sweep++) {\n    if (tid == 0u) {\n      var max_abs = ${u}(1);\n      var max_offdiag = ${u}(0);\n      for (var idx = 0u; idx < ${a*a}u; idx++) {\n        let row = idx / ${a}u;\n        let col = idx % ${a}u;\n        let value = abs(diagonalized[base + idx]);\n        max_abs = max(max_abs, value);\n        if (row > col) {\n          max_offdiag = max(max_offdiag, value);\n        }\n      }\n      done = select(0u, 1u, max_offdiag <= ${l} * max_abs);\n    }\n    let done_uniform = workgroupUniformLoad(&done);\n    if (done_uniform != 0u) {\n      break;\n    }\n\n    for (var p = 0u; p + 1u < ${a}u; p++) {\n      for (var q = p + 1u; q < ${a}u; q++) {\n        if (tid == 0u) {\n          rot_app = diagonalized[mat_idx(base, p, p)];\n          rot_aqq = diagonalized[mat_idx(base, q, q)];\n          rot_apq = diagonalized[sym_idx(base, p, q)];\n          if (rot_apq == ${u}(0)) {\n            rot_active = 0u;\n            rot_c = ${u}(1);\n            rot_s = ${u}(0);\n          } else {\n            let tau = (rot_aqq - rot_app) / (${u}(2) * rot_apq);\n            let tau_sign = select(${u}(-1), ${u}(1), tau >= ${u}(0));\n            let t = tau_sign / (abs(tau) + sqrt(tau * tau + ${u}(1)));\n            rot_c = inverseSqrt(t * t + ${u}(1));\n            rot_s = t * rot_c;\n            rot_active = 1u;\n          }\n        }\n        workgroupBarrier();\n\n        if (rot_active != 0u) {\n          for (var k = tid; k < ${a}u; k += ${d}u) {\n            if (k != p && k != q) {\n              let kp = sym_idx(base, k, p);\n              let kq = sym_idx(base, k, q);\n              let akp = diagonalized[kp];\n              let akq = diagonalized[kq];\n              let next_kp = rot_c * akp - rot_s * akq;\n              let next_kq = rot_s * akp + rot_c * akq;\n              diagonalized[kp] = next_kp;\n              diagonalized[kq] = next_kq;\n            } else if (k == p) {\n              diagonalized[mat_idx(base, p, p)] =\n                rot_c * rot_c * rot_app - ${u}(2) * rot_s * rot_c * rot_apq + rot_s * rot_s * rot_aqq;\n              diagonalized[sym_idx(base, p, q)] = ${u}(0);\n            } else {\n              diagonalized[mat_idx(base, q, q)] =\n                rot_s * rot_s * rot_app + ${u}(2) * rot_s * rot_c * rot_apq + rot_c * rot_c * rot_aqq;\n            }\n\n            let vp = mat_idx(base, k, p);\n            let vq = mat_idx(base, k, q);\n            let vkp = vectors[vp];\n            let vkq = vectors[vq];\n            vectors[vp] = rot_c * vkp - rot_s * vkq;\n            vectors[vq] = rot_s * vkp + rot_c * vkq;\n          }\n        }\n        storageBarrier();\n      }\n    }\n  }\n}\n`.trim(),numInputs:1,numOutputs:2,hasUniform:!1,passes:[{grid:E(s)}]}]}function F(e,t,n,r){return new Uint8Array(new Uint32Array([e,t,n,r?1:0]).buffer)}function ke(e,t,n){const r=t.inputDtypes[0],i=t.inputShapes[0],a=i[i.length-1],s=q(i.slice(0,-1));if(q(n.factors)!==a)throw new Error(`fft: factorization ${n.factors} does not match size ${a}`);const o="float16"===r,u=w(r,!0),l=Math.min(256,C(0,e.limits.maxComputeWorkgroupSizeX)),d=Math.max(1,...n.factors),p=n.inverse?"6.283185307179586":"-6.283185307179586",c=n.factors.map((e=>`\n  digit = remaining % ${e}u;\n  remaining = remaining / ${e}u;\n  stride = stride * ${e}u;\n  reversed = reversed + digit * (${a}u / stride);`)).join(""),f=`\n${o?"enable f16;":""}\n${z}\n\n@group(0) @binding(0) var<storage, read> input_real: array<${u}>;\n@group(0) @binding(1) var<storage, read> input_imag: array<${u}>;\n@group(0) @binding(2) var<storage, read_write> output_real: array<${u}>;\n@group(0) @binding(3) var<storage, read_write> output_imag: array<${u}>;\n\nstruct FftParams {\n  phase: u32,\n  radix: u32,\n  prev: u32,\n  normalize: u32,\n}\n\n@group(1) @binding(0) var<uniform> fft_params: FftParams;\n\nfn digit_reversed_index(index: u32) -> u32 {\n  var remaining = index;\n  var stride = 1u;\n  var reversed = 0u;\n  var digit = 0u;\n${c}\n  return reversed;\n}\n\n@compute @workgroup_size(${l})\nfn main(@builtin(global_invocation_id) global_id: vec3<u32>) {\n  let global = global_id.x + global_id.y * ${M*l}u;\n\n  if (fft_params.phase == 0u) {\n    if (global >= ${s*a}u) {\n      return;\n    }\n    let batch = global / ${a}u;\n    let out_idx = global % ${a}u;\n    let source = batch * ${a}u + digit_reversed_index(out_idx);\n    output_real[global] = input_real[source];\n    output_imag[global] = input_imag[source];\n    return;\n  }\n\n  let butterflies_per_batch = ${a}u / fft_params.radix;\n  if (global >= ${s}u * butterflies_per_batch) {\n    return;\n  }\n\n  let batch = global / butterflies_per_batch;\n  let local = global % butterflies_per_batch;\n  let j = local % fft_params.prev;\n  let group = local / fft_params.prev;\n  let span = fft_params.prev * fft_params.radix;\n  let start = batch * ${a}u + group * span + j;\n  let scale = select(1.0, 1.0 / f32(${a}u), fft_params.normalize != 0u);\n\n  var scratch_real: array<f32, ${d}>;\n  var scratch_imag: array<f32, ${d}>;\n\n  for (var q = 0u; q < fft_params.radix; q++) {\n    let idx = start + q * fft_params.prev;\n    let angle = ${p} * f32(q * j) / f32(span);\n    let c = cos(angle);\n    let s = sin(angle);\n    let xr = f32(output_real[idx]);\n    let xi = f32(output_imag[idx]);\n    scratch_real[q] = xr * c - xi * s;\n    scratch_imag[q] = xr * s + xi * c;\n  }\n\n  for (var p = 0u; p < fft_params.radix; p++) {\n    var sum_real = 0.0;\n    var sum_imag = 0.0;\n    for (var q = 0u; q < fft_params.radix; q++) {\n      let angle = ${p} * f32(q * p) / f32(fft_params.radix);\n      let c = cos(angle);\n      let s = sin(angle);\n      let xr = scratch_real[q];\n      let xi = scratch_imag[q];\n      sum_real += xr * c - xi * s;\n      sum_imag += xr * s + xi * c;\n    }\n    let idx = start + p * fft_params.prev;\n    output_real[idx] = ${u}(sum_real * scale);\n    output_imag[idx] = ${u}(sum_imag * scale);\n  }\n}\n`.trim(),h=[{grid:E(Math.ceil(s*a/l)),uniform:F(0,1,1,!1)}];let x=1;for(let e=0;e<n.factors.length;e++){const t=n.factors[e];h.push({grid:E(Math.ceil(s*a/t/l)),uniform:F(1,t,x,n.inverse&&e===n.factors.length-1)}),x*=t}return[{code:f,numInputs:2,numOutputs:2,hasUniform:!0,passes:h}]}function D(e,t){switch(t.name){case"Sort":return _e(e,t.type);case"Argsort":return $e(e,t.type);case"TriangularSolve":return be(e,t.type,t.params);case"Cholesky":return we(e,t.type);case"LU":return ve(e,t.type);case"JacobiEigh":return ye(e,t.type,t.params);case"Fft":return ke(e,t.type,t.params);default:throw new ie(t.name,"webgpu")}}function Se(e){return{querySet:e.createQuerySet({type:"timestamp",count:T}),resolve:e.createBuffer({size:8*T,usage:GPUBufferUsage.QUERY_RESOLVE|GPUBufferUsage.COPY_SRC}),dst:e.createBuffer({size:8*T,usage:GPUBufferUsage.MAP_READ|GPUBufferUsage.COPY_DST}),nextIndex:0,entries:[]}}function je(e,t,n,r,i){const a=ae(n);a.properties.push(["passes",`${r}`]),a.properties.push(["source",i]),t.batch.entries.push({...a,beginIndex:t.beginIndex,endIndex:t.endIndex}),Ue(e)}function Ue(e){queueMicrotask((()=>{const t=N.get(e);t&&t.entries.length>0&&(Be(e,t),N.set(e,Se(e)))}))}function Be(e,t){if(0===t.entries.length)return;const n=t.nextIndex,r=e.createCommandEncoder();r.resolveQuerySet(t.querySet,0,n,t.resolve,0),r.copyBufferToBuffer(t.resolve,0,t.dst,0,8*n),e.queue.submit([r.finish()]);const{entries:i}=t;t.dst.mapAsync(GPUMapMode.READ).then((()=>{try{const e=new BigInt64Array(t.dst.getMappedRange()),n=e[i[i.length-1].endIndex],r=performance.now();for(const t of i)oe("webgpu",t,r+Number(e[t.beginIndex]-n)/1e6,r+Number(e[t.endIndex]-n)/1e6)}finally{t.dst.unmap(),t.querySet.destroy(),t.resolve.destroy(),t.dst.destroy()}}))}function Ee(e,t){const n=me(e,t);if(n)return n;const r=re(t),{nargs:i,reduction:a}=t,s=Array.from({length:i},((e,t)=>`in${t}`)),o=new V;o.emitPreamble(e,[r.exp,r.epilogue]);const u=Array.from({length:i},(()=>null));r.exp.fold((e=>{"GlobalIndex"===e.op&&(u[e.arg[0]]=e.dtype)})),r.epilogue?.fold((e=>{"GlobalIndex"===e.op&&(u[e.arg[0]]=e.dtype)}));for(let e=0;e<i;e++){const t=w(u[e]??"float32",!0);o.emit(`@group(0) @binding(${e}) var<storage, read> ${s[e]} : array<${t}>;`)}const l=w(t.dtype,!0);o.emit(`@group(0) @binding(${i}) var<storage, read_write> result : array<${l}>;`);const d=a?r.size.groups??1:1,p=a&&d>1;if(p&&r.threadCount%d!=0)throw new Error("WebGPU grouped reduction has invalid thread count");if(p&&d>e.limits.maxComputeWorkgroupSizeX)throw new Error("WebGPU grouped reduction exceeds workgroup size limit");const c=p?d:C(r.threadCount,256),f=p?r.threadCount/d:Math.ceil(r.threadCount/c),[x,m]=E(f);if(p){const e=w(a.dtype);for(let t=0;t<(r.size.upcast??1);t++)o.emit(`var<workgroup> partial${t}: array<${e}, ${d}>;`)}if(o.emit("",`@compute @workgroup_size(${c})`),p)o.emit("fn main(",o.pushIndent,"@builtin(local_invocation_id) lid : vec3<u32>,","@builtin(workgroup_id) wg_id : vec3<u32>,",o.popIndent,") {",o.pushIndent),1===m?o.emit(`if (wg_id.x >= ${f}u) { return; }`,"let gidx: i32 = i32(wg_id.x);"):o.emit(`if (${x}u * wg_id.y + wg_id.x >= ${f}u) { return; }`,`let gidx: i32 = i32(${x}u * wg_id.y + wg_id.x);`),o.emit("let group: i32 = i32(lid.x);");else if(o.emit("fn main(@builtin(global_invocation_id) id : vec3<u32>) {",o.pushIndent),1===m)o.emit(`if (id.x >= ${r.threadCount}) { return; }`,"let gidx: i32 = i32(id.x);");else{const e=x*c;o.emit(`if (${e} * id.y + id.x >= ${r.threadCount}) { return; }`,`let gidx: i32 = i32(${e} * id.y + id.x);`)}o.emitPhonyAssignments(s);const g=new Z(o,s);if(a){const e=r.size.unroll??1,t=r.size.upcast??1,n=[...Array(t)].map(((e,t)=>`acc${t}`));for(let e=0;e<t;e++)o.emit(`var ${n[e]}: ${w(a.dtype)} = ${Q(a.dtype,a.identity)};`);o.emit(`for (var ridx: i32 = 0; ridx < ${r.size.reduce}; ridx++) {`,o.pushIndent);const i=[],s=new Map;for(let n=0;n<t;n++){i.push([]);for(let t=0;t<e;t++){const e=r.exp.substitute({upcast:U.i32(n),unroll:U.i32(t)});i[n].push(e.simplify(s)),g.countReferences(i[n][t])}}const u=i.map((e=>e.map((e=>g.run(e))).map(h)));for(let r=0;r<t;r++){let t=u[r][0];for(let n=1;n<e;n++)if("Add"===a.op)t=`${t} + ${u[r][n]}`;else if("Mul"===a.op)t=`${t} * ${u[r][n]}`;else if("Min"===a.op)t="bool"===a.dtype?`(${t} && ${u[r][n]})`:`min(${t}, ${u[r][n]})`;else{if("Max"!==a.op)throw new Error(`Unsupported reduction op: ${a.op}`);t="bool"===a.dtype?`(${t} || ${u[r][n]})`:`max(${t}, ${u[r][n]})`}if("Add"===a.op)o.emit(`${n[r]} += ${t};`);else if("Mul"===a.op)o.emit(`${n[r]} *= ${t};`);else if("Min"===a.op)"bool"===a.dtype?o.emit(`${n[r]} = ${n[r]} && ${t};`):o.emit(`${n[r]} = min(${n[r]}, ${t});`);else{if("Max"!==a.op)throw new Error(`Unsupported reduction op: ${a.op}`);"bool"===a.dtype?o.emit(`${n[r]} = ${n[r]} || ${t};`):o.emit(`${n[r]} = max(${n[r]}, ${t});`)}}if(o.emit(o.popIndent,"}"),p){for(let e=0;e<t;e++)o.emit(`partial${e}[lid.x] = ${n[e]};`);o.emit("workgroupBarrier();");for(let e=d/2;e>=1;e/=2){o.emit(`if (lid.x < ${e}u) {`,o.pushIndent);for(let n=0;n<t;n++)o.emit(`partial${n}[lid.x] = ${le(a.op,a.dtype,`partial${n}[lid.x]`,`partial${n}[lid.x + ${e}u]`)};`);o.emit(o.popIndent,"}","workgroupBarrier();")}}g.reset();const c=[],f=[];for(let e=0;e<t;e++){const t=r.outputIdxExp.substitute({upcast:U.i32(e)});c.push(t.simplify(s)),g.countReferences(c[e]),f.push(r.epilogue.substitute({acc:U.variable(a.dtype,n[e]),upcast:U.i32(e)}).simplify(s)),g.countReferences(f[e])}if(p){o.emit("if (lid.x == 0u) {",o.pushIndent);for(let e=0;e<t;e++)o.emit(`${n[e]} = partial${e}[0u];`)}for(let e=0;e<t;e++){const t=h(g.run(c[e]));let n=h(g.run(f[e]));l!==w(f[e].dtype)&&(n=`${l}(${n})`),o.emit(`result[${t}] = ${n};`)}p&&o.emit(o.popIndent,"}")}else{g.countReferences(r.exp);let e=h(g.run(r.exp));l!==w(r.exp.dtype)&&(e=`${l}(${e})`),o.emit(`result[gidx] = ${e};`)}return o.emit(o.popIndent,"}"),{code:o.toString(),numInputs:i,numOutputs:1,hasUniform:!1,passes:[{grid:[x,m]}]}}function Pe(e,t,n,r){const i=e.createCommandEncoder();H(e,i,t,n,r),e.queue.submit([i.finish()])}function H(e,t,n,r,i){const{data:a,source:s}=n;for(const{pipeline:n,...o}of a){if(r.length!==o.numInputs||i.length!==o.numOutputs)throw new Error(`webgpu: expected ${o.numInputs} inputs and ${o.numOutputs} outputs, got ${r.length} inputs and ${i.length} outputs`);const a=o.passes.filter((({grid:e})=>q(e)>0));if(0===a.length)continue;const u=void 0,l=e.createBindGroup({layout:n.getBindGroupLayout(0),entries:[...r.map(((e,t)=>({binding:t,resource:{buffer:e}}))),...i.map(((e,t)=>({binding:r.length+t,resource:{buffer:e}})))]});let d=null,p=0;if(o.hasUniform){const[t,r]=Me(e,a.map((({uniform:e})=>e)));p=r,d=e.createBindGroup({layout:n.getBindGroupLayout(1),entries:[{binding:0,resource:{buffer:t,size:r}}]})}for(let e=0;e<a.length;e++){const{grid:r}=a[e];let i;if(u){const t=0===e,n=e===a.length-1;(t||n)&&(i={querySet:u.batch.querySet,...t?{beginningOfPassWriteIndex:u.beginIndex}:{},...n?{endOfPassWriteIndex:u.endIndex}:{}})}const s=t.beginComputePass({timestampWrites:i});s.setPipeline(n),s.setBindGroup(0,l),d&&s.setBindGroup(1,d,[e*p]),s.dispatchWorkgroups(r[0],r[1]),s.end()}u&&je(e,u,s,a.length,o.code)}}function Me(e,t){for(const e of t)if(!e||0===e.byteLength||e.byteLength!==t[0].byteLength)throw new Error("webgpu: Uniform mismatch between shader passes");const n=e.limits.minUniformBufferOffsetAlignment,r=Math.ceil(t[0].byteLength/n)*n,i=e.createBuffer({size:r*t.length,usage:GPUBufferUsage.UNIFORM,mappedAtCreation:!0}),a=new Uint8Array(i.getMappedRange());for(let e=0;e<t.length;e++)a.set(t[e],e*r);return i.unmap(),[i,r]}async function Y(e,t,n){let r=`Failed to compile shader: ${t?t.message:"(no error scope)"}`;const i=await e.getCompilationInfo();for(const e of i.messages)r+=`\n  [${e.type} at ${e.lineNum}:${e.linePos}] ${e.message}`;return n&&(r+=`\n\n${n}`),r}import{S as L,F as ee,E as G,r as te,t as re,f as C,s as h,A as U,U as ie,p as q,m as se,a as O,i as R,b as W,c as ae,e as oe}from"./fimd-Bz3YK2ya.js"
;const ne="\nfn threefry2x32(key: vec2<u32>, ctr: vec2<u32>) -> vec2<u32> {\n  let ks0: u32 = key.x;\n  let ks1: u32 = key.y;\n  let ks2: u32 = ks0 ^ ks1 ^ 0x1BD11BDAu;\n\n  var x0: u32 = ctr.x + ks0;\n  var x1: u32 = ctr.y + ks1;\n\n  x0 += x1; x1 = (x1 << 13u) | (x1 >> 19u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 15u) | (x1 >> 17u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 26u) | (x1 >> 6u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 6u) | (x1 >> 26u); x1 ^= x0;\n  x0 += ks1;\n  x1 += ks2 + 1u;\n\n  x0 += x1; x1 = (x1 << 17u) | (x1 >> 15u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 29u) | (x1 >> 3u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 16u) | (x1 >> 16u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 24u) | (x1 >> 8u); x1 ^= x0;\n  x0 += ks2;\n  x1 += ks0 + 2u;\n\n  x0 += x1; x1 = (x1 << 13u) | (x1 >> 19u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 15u) | (x1 >> 17u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 26u) | (x1 >> 6u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 6u) | (x1 >> 26u); x1 ^= x0;\n  x0 += ks0;\n  x1 += ks1 + 3u;\n\n  x0 += x1; x1 = (x1 << 17u) | (x1 >> 15u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 29u) | (x1 >> 3u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 16u) | (x1 >> 16u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 24u) | (x1 >> 8u); x1 ^= x0;\n  x0 += ks1;\n  x1 += ks2 + 4u;\n\n  x0 += x1; x1 = (x1 << 13u) | (x1 >> 19u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 15u) | (x1 >> 17u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 26u) | (x1 >> 6u); x1 ^= x0;\n  x0 += x1; x1 = (x1 << 6u) | (x1 >> 26u); x1 ^= x0;\n  x0 += ks2;\n  x1 += ks0 + 5u;\n\n  return vec2<u32>(x0, x1);\n}",ue="\nconst _erf_p: f32 = 0.3275911;\nconst _erf_a1: f32 = 0.254829592;\nconst _erf_a2: f32 = -0.284496736;\nconst _erf_a3: f32 = 1.421413741;\nconst _erf_a4: f32 = -1.453152027;\nconst _erf_a5: f32 = 1.061405429;\nfn erf(x: f32) -> f32 {\n  let t = 1.0 / (1.0 + _erf_p * abs(x));\n  let P_t = fma(fma(fma(fma(_erf_a5, t, _erf_a4), t, _erf_a3), t, _erf_a2), t, _erf_a1) * t;\n  return sign(x) * (1.0 - P_t * exp(-x * x));\n}\nfn erfc(x: f32) -> f32 {\n  let t = 1.0 / (1.0 + _erf_p * abs(x));\n  let P_t = fma(fma(fma(fma(_erf_a5, t, _erf_a4), t, _erf_a3), t, _erf_a2), t, _erf_a1) * t;\n  let E = P_t * exp(-x * x);\n  return select(2.0 - E, E, x >= 0.0);\n}",z=String.raw`
fn nan() -> f32 { let bits = 0xffffffffu; return bitcast<f32>(bits); }
fn inf() -> f32 { let bits = 0x7f800000u; return bitcast<f32>(bits); }
`.trim();var V=class{pushIndent=Symbol("pushIndent");popIndent=Symbol("popIndent");lines=[];#e="";emit(...e){for(const t of e)t===this.pushIndent?this.#e+="  ":t===this.popIndent?this.#e=this.#e.slice(0,-2):this.lines.push(t?this.#e+t:"")}emitPreamble(e,t){let n=!1,r=new Map;for(const e of t)null!=e&&(n||=e.some((e=>"float16"===e.dtype)),r=se(r,e.distinctOps()));if(n){if(!e.features.has("shader-f16"))throw new Error("WebGPU device does not support shader-f16 feature");this.emit("enable f16;")}this.emit(z),r.has("Threefry2x32")&&this.emit(ne),(r.has("Erf")||r.has("Erfc"))&&this.emit(ue),this.emit("")}emitPhonyAssignments(e){e.length>0&&this.emit(e.map((e=>`_ = &${e};`)).join(" "))}toString(){return this.lines.join("\n")}},Z=class{wb;args;#e=0;#t=new Map;#n=new Set;#r=new Map;constructor(e,t){this.wb=e,this.args=t}#i(){return"alu"+this.#e++}#a(e){return e.match(/^alu[0-9]+$/)}countReferences(e){if(this.#t.set(e,(this.#t.get(e)??0)+1),!this.#n.has(e)){this.#n.add(e);for(const t of e.src)this.countReferences(t)}}reset(){this.#t.clear(),this.#n.clear(),this.#r.clear()}run(e){if(this.#r.has(e))return this.#r.get(e);const{op:t,src:n,dtype:r,arg:i}=e;let a="";if(O.Binary.has(t)||O.Compare.has(t)){const e=this.run(n[0]),s=this.run(n[1]);if("Add"===t)a="bool"===r?`(${e} || ${s})`:`(${e} + ${s})`;else if("Sub"===t)a=`(${e} - ${s})`;else if("Mul"===t)a="bool"===r?`(${e} && ${s})`:`(${e} * ${s})`;else if("Idiv"===t)a=R(r)?`trunc(${e} / ${s})`:`(${e} / ${s})`;else if("Mod"===t)a=`(${e} % ${s})`;else if("Min"===t)a="bool"===r?`(${e} && ${s})`:`min(${h(e)}, ${h(s)})`;else if("Max"===t)a="bool"===r?`(${e} || ${s})`:`max(${h(e)}, ${h(s)})`;else if("BitCombine"===t)a="and"===i?`(${e} & ${s})`:"or"===i?`(${e} | ${s})`:"bool"===r?`(${e} != ${s})`:`(${e} ^ ${s})`;else if("BitShift"===t)a="shl"===i?`(${e} << ${s})`:`(${e} >> ${s})`;else if("Cmplt"===t)a=`(${e} < ${s})`;else if("Cmpne"===t)if(R(n[0].dtype)){const t=this.#a(e)?e:this.#i();t!==e&&this.wb.emit(`let ${t} = ${e};`),a=`(${t} != ${s} || min(${t}, ${w(n[0].dtype)}(inf())) != ${t})`}else a=`(${e} != ${s})`}else if(O.Unary.has(t))if("Reciprocal"===t&&"Sqrt"===n[0].op)a=`inverseSqrt(${this.run(n[0].src[0])})`;else{const e=this.run(n[0]);if("Sin"===t)a=`sin(${h(e)})`;else if("Cos"===t)a=`cos(${h(e)})`;else if("Asin"===t)a=`asin(${h(e)})`;else if("Atan"===t)a=`atan(${h(e)})`;else if("Exp"===t)a=`exp(${h(e)})`;else if("Log"===t)a=`log(${h(e)})`;else if("Erf"===t||"Erfc"===t){const n="Erf"===t?"erf":"erfc";a="float32"!==r?`${w(r)}(${n}(f32(${h(e)})))`:`${n}(${h(e)})`}else if("Sqrt"===t)a=`sqrt(${h(e)})`;else if("Reciprocal"===t)a=`(1.0 / ${e})`;else if("Floor"===t)a=`floor(${h(e)})`;else if("Ceil"===t)a=`ceil(${h(e)})`;else if("Cast"===t){const t=w(n[0].dtype),i=w(r);if(R(n[0].dtype)&&!R(r)&&"bool"!==r){const n=K(r),s=this.#a(e)?e:this.#i();s!==e&&this.wb.emit(`let ${s}: ${t} = ${h(e)};`),a=`select(${i}(${s}), ${n}, ${s} >= ${t}(${n}))`}else a=`${i}(${h(e)})`}else"Bitcast"===t&&(a=`bitcast<${w(r)}>(${h(e)})`)}else if("Where"===t)a=`select(${h(this.run(n[2]))}, ${h(this.run(n[1]))}, ${h(this.run(n[0]))})`;else if("Threefry2x32"===t){const e=this.#i(),[s,o,u,l]=n.map((e=>h(this.run(e))));if(this.wb.emit(`let ${e} = threefry2x32(vec2(${s}, ${o}), vec2(${u}, ${l}));`),"xor"===i)a=`(${e}.x ^ ${e}.y)`;else if(0===i)a=`${e}.x`;else{if(1!==i)throw new W(t,r,"webgpu",i);a=`${e}.y`}}else{if("Const"===t)return Q(r,i);if("Special"===t)return i[0];if("Variable"===t)return i;"GlobalIndex"===t&&(a=`${this.args[i[0]]}[${h(this.run(n[0]))}]`,"bool"===r&&(a=`(${a} != 0)`))}if(!a)throw new W(t,r,"webgpu",i);const s=w(r);if((this.#t.get(e)??0)>1){const t=this.#i();return this.#r.set(e,t),this.wb.emit(`let ${t}: ${s} = ${h(a)};`),t}return this.#r.set(e,a),a}};const M=16384;var xe=class e{device;static alphaModes=["opaque","premultiplied"];static width=256;static height=256;initialized=!1;deviceStorage;deviceContexts;hostStorage;hostContext;constructor(e){this.device=e}#e(){if(typeof OffscreenCanvas>"u")throw new Error("OffscreenCanvas is not available in this environment, so you cannot read data from WebGPU synchronously. Consider using the async API.");const t=()=>new OffscreenCanvas(e.width,e.height);this.deviceStorage=e.alphaModes.map(t),this.deviceContexts=this.deviceStorage.map(((t,n)=>{const r=t.getContext("webgpu");return r.configure({device:this.device,format:"bgra8unorm",usage:GPUTextureUsage.COPY_DST,alphaMode:e.alphaModes[n]}),r})),this.hostStorage=t(),this.hostContext=this.hostStorage.getContext("2d",{willReadFrequently:!0}),this.initialized=!0}read(t,n,r){this.initialized||this.#e();const i=this.deviceStorage,a=this.deviceContexts,s=this.hostContext,o=Math.ceil(r/4),u=4*e.width,l=new ArrayBuffer(4*o);for(let r=0;r<a.length;r++){const d=a[r].getCurrentTexture(),p=(a,o,p)=>{const c=this.device.createCommandEncoder();c.copyBufferToTexture({buffer:t,bytesPerRow:u,offset:p+n},{texture:d},{width:a,height:o,depthOrArrayLayers:1});const f=c.finish();this.device.queue.submit([f]),s.clearRect(0,0,a,o),s.drawImage(i[r],0,0);const h=s.getImageData(0,0,a,o).data,x=new Uint8ClampedArray(l,p,4*a*o),m=e.alphaModes[r];for(let e=0;e<x.length;e+=4)"premultiplied"===m?x[e+3]=h[e+3]:(x[e]=h[e+2],x[e+1]=h[e+1],x[e+2]=h[e])},c=e.width*e.height,f=Math.floor(o/c);let h=o%c;const x=Math.floor(h/e.width);h%=e.width;let m=0;for(let t=0;t<f;t++)p(e.width,e.height,m),m+=4*c;x>0&&(p(e.width,x,m),m+=x*e.width*4),h>0&&p(h,1,m)}return new Uint8Array(l,0,r)}};const T=4096,N=new WeakMap,X=67108864,qe=64;var ze=class{device;type="webgpu";maxArgs;pipelines;syncReader;buffers;nextSlot;#e=new Map;#t;#n=new Map;constructor(e){this.device=e,this.maxArgs=this.device.limits.maxStorageBuffersPerShaderStage-1,this.pipelines=new Ce(e),this.syncReader=new xe(e),this.buffers=new Map,this.nextSlot=1,this.#t=this.#s(4),e.addEventListener("uncapturederror",(()=>{}))}malloc(e,t){if(t&&t.byteLength!==e)throw new Error("initialData size does not match buffer size");const n=4*Math.ceil(e/4)||4,r=0===e?this.#t:this.#a(n);if(t&&e>0)if(t.byteLength%4==0)this.device.queue.writeBuffer(r,0,t);else{const e=t.byteLength-t.byteLength%4;e>0&&this.device.queue.writeBuffer(r,0,t,0,e);const n=new Uint8Array(4);n.set(t.subarray(e)),this.device.queue.writeBuffer(r,e,n)}const i=this.nextSlot++;return this.buffers.set(i,{buffer:r,size:e,allocatedSize:n,ref:1}),i}incRef(e){const t=this.buffers.get(e);if(!t)throw new L(e);t.ref++}decRef(e){const t=this.buffers.get(e);if(!t)throw new L(e);t.ref--,0===t.ref&&(this.buffers.delete(e),t.buffer!==this.#t&&this.#o(t.buffer,t.allocatedSize))}async read(e,t,n){const{buffer:r,size:i}=this.#i(e);if(r===this.#t)return new Uint8Array;void 0===t&&(t=0),void 0===n&&(n=i-t);const a=4*Math.ceil(n/4),s=this.#s(a,{read:!0});try{const e=this.device.createCommandEncoder();e.copyBufferToBuffer(r,t,s,0,a),this.device.queue.submit([e.finish()]),await s.mapAsync(GPUMapMode.READ);const i=s.getMappedRange();return new Uint8Array(i.slice(),0,n)}finally{s.destroy()}}readSync(e,t,n){const{buffer:r,size:i}=this.#i(e);return r===this.#t?new Uint8Array:(void 0===t&&(t=0),void 0===n&&(n=i-t),this.syncReader.read(r,t,n))}#r(e){const t=ee.hash(e);let n=this.#e.get(t);return n||(n=Ee(this.device,e),this.#e.set(t,n)),n}async prepareKernel(e){const t=this.#r(e),n=await this.pipelines.prepare(t);return new G(e,[{...t,pipeline:n}])}prepareKernelSync(e){const t=this.#r(e),n=this.pipelines.prepareSync(t);return new G(e,[{...t,pipeline:n}])}async prepareRoutine(e){const t=D(this.device,e);return new G(e,await Promise.all(t.map((async e=>{const t=await this.pipelines.prepare(e);return{...e,pipeline:t}}))))}prepareRoutineSync(e){return new G(e,D(this.device,e).map((e=>{const t=this.pipelines.prepareSync(e);return{...e,pipeline:t}})))}dispatch(e,t,n){const r=t.map((e=>this.#i(e).buffer)),i=n.map((e=>this.#i(e).buffer));Pe(this.device,e,r,i)}dispatchBatch(e){const t=e.map((({exe:e,inputs:t,outputs:n})=>({exe:e,inputs:t.map((e=>this.#i(e).buffer)),outputs:n.map((e=>this.#i(e).buffer))}))),n=1024;for(let e=0;e<t.length;e+=n){const r=this.device.createCommandEncoder();for(const i of t.slice(e,e+n))H(this.device,r,i.exe,i.inputs,i.outputs);this.device.queue.submit([r.finish()])}}#i(e){const t=this.buffers.get(e);if(!t)throw new L(e);return{buffer:t.buffer,size:t.size}}#a(e){if(e>X)return this.#s(e);const t=this.#n.get(e),n=t?.pop();return t&&0===t.length&&this.#n.delete(e),n??this.#s(e)}#o(e,t){if(t>X)return void e.destroy();const n=this.#n.get(t);n?n.length>=qe?e.destroy():n.push(e):this.#n.set(t,[e])}#s(e,{mapped:t=!1,read:n=!1}={}){if(n&&t)throw new Error("mapped and read cannot both be true");return this.device.createBuffer({size:e,usage:n?GPUBufferUsage.MAP_READ|GPUBufferUsage.COPY_DST:GPUBufferUsage.STORAGE|GPUBufferUsage.COPY_SRC|GPUBufferUsage.COPY_DST,mappedAtCreation:t})}},Ce=class{device;cache;inProgress;constructor(e){this.device=e,this.cache=new Map,this.inProgress=new Map}#e(e){if(e.numInputs+e.numOutputs>this.device.limits.maxStorageBuffersPerShaderStage){const t=e.numInputs+e.numOutputs,n=this.device.limits.maxStorageBuffersPerShaderStage;throw new Error(`Too many buffers (${t}) for WebGPU pipeline (max: ${n})`)}const t=[this.device.createBindGroupLayout({entries:te(e.numInputs+e.numOutputs).map((t=>({binding:t,visibility:GPUShaderStage.COMPUTE,buffer:{type:t<e.numInputs?"read-only-storage":"storage"}})))})];return e.hasUniform&&t.push(this.device.createBindGroupLayout({entries:[{binding:0,visibility:GPUShaderStage.COMPUTE,buffer:{type:"uniform",hasDynamicOffset:!0}}]})),this.device.createPipelineLayout({bindGroupLayouts:t})}async prepare(e){const t=this.cache.get(e.code);if(t)return t;const n=this.inProgress.get(e.code);if(n)return await n;const r=this.device.createShaderModule({code:e.code}),i=(async()=>{this.device.pushErrorScope("validation");try{const t=await this.device.createComputePipelineAsync({layout:this.#e(e),compute:{module:r,entryPoint:"main"}});return await this.device.popErrorScope(),t}catch{const t=await Y(r,await this.device.popErrorScope(),e.code);throw new Error(t)}})();this.inProgress.set(e.code,i);const a=await i;return this.cache.set(e.code,a),a}prepareSync(e){const t=this.cache.get(e.code);if(t)return t;const n=this.device.createShaderModule({code:e.code});this.device.pushErrorScope("validation");const r=this.device.createComputePipeline({layout:this.#e(e),compute:{module:n,entryPoint:"main"}});return this.device.popErrorScope().then((async t=>{if(null!==t){await Y(n,t,e.code)}})),this.cache.set(e.code,r),r}};export{ze as WebGPUBackend};